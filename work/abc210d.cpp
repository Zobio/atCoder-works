#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ull unsigned long long
#define MOD 1000000007

int main() {
	int h, w;
	cin >> h >> w;
	ll c;
	cin >> c;
	vector<vector<ll>> a(h, vector<ll>(w));
	for(int i = 0; i < h; i++) {
		for(int j = 0; j < w; j++) {
			cin >> a[i][j];
		}
	}
	ll ans = (ll)200000000000000000;
	for(int i = 0; i < h; i++) {
		
	}
}